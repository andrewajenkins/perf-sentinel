function generateReport(report) {
  console.log('PR Comment reporter is not yet implemented.');
  // This would use a library like @octokit/rest to post a comment to a PR.
}

module.exports = { generateReport }; 