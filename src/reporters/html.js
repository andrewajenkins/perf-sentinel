function generateReport(report) {
  console.log('HTML reporter is not yet implemented.');
  // This would generate a self-contained HTML file with charts and tables.
}

module.exports = { generateReport }; 